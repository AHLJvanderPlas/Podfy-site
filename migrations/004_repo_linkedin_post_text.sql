-- 004 (podfy-public): whitepaper ad-hoc channel — generated post text stored
-- on the repository item until the admin clicks "Publish now".
ALTER TABLE repository_items ADD COLUMN linkedin_post_text TEXT;
